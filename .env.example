# Server-only. Never prefix with VITE_ or NEXT_PUBLIC_.
# Optional: without it, portfolio holdings still load via public Solana RPC.
# Transaction history and cost basis are richer when this is set.
HELIUS_API_KEY=your_key_here
